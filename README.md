trident is a (likely backdoored) spnet block cipher written in C



how to use:
```mkdir target && cd target ; cmake .. && make && ./trident keygen```

- you need openssl lib for the hash functions. alternatively replace it with a full impl of it yourself


wasm now


### pending rewrite in golang; current codebase is too messy
